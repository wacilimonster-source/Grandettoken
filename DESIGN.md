# TokenScope 产品设计文档

> 版本 1.0 · 2026-09-17
> 适用范围:常驻 Windows 桌面的多平台 AI Token 余额与用量监控小组件
> 配套文档:[`PROGRESS.md`](PROGRESS.md)(实现进展与踩坑记录) · [`app/README.md`](app/README.md)(开发者说明) · [`token-dashboard-prototype.html`](token-dashboard-prototype.html)(视觉原型稿) · [`design-spec.html`](design-spec.html)(本文档的可视化版) · [`bug-report-2026-09-17.html`](bug-report-2026-09-17.html)(缺陷审查)
>
> 本文档记录**已实现的产品设计**(而不是设想),包含四部分:设计基线、已开发功能清单、设计稿差异台账、优化建议。

---

## 1. 产品定位

### 1.1 一句话

常驻桌面、不打扰、一眼看出"哪个 API Key 要没钱了"的余额挂件。

### 1.2 目标用户

同时使用多个 AI 编码/对话服务(4SAPI 中转、OpenCode Go 订阅、DeepSeek、Hapi 等)的开发者。特征是:渠道多、余额分散、单渠道告警邮件容易被忽略、不希望为了看一眼余额去开四个网页控制台。

### 1.3 核心价值(按优先级)

| 优先级 | 价值 | 落到产品上的形态 |
|---|---|---|
| 1 | **不误报** | 取数失败绝不渲染成"余额归零";沿用上次成功快照并标注时间与真实错误原因 |
| 2 | **一屏看清** | 四个渠道在一屏内(面板 380×560),主数值直接染状态色,视线自然落到最紧张的那个 |
| 3 | **不打扰** | 四形态折叠,最窄可收成 8×64 的纯色竖条贴在屏幕边缘 |
| 4 | **可解释** | 每个数字都能点开看到口径:消耗是推算值、百分比的分母是什么、上次申请是什么时候 |
| 5 | **零信任成本** | 密钥只进 Windows 凭据管理器,配置文件可随意备份分享;无遥测、无云同步 |

### 1.4 明确不做(非目标)

- **不做推理/对话入口**。只做账务展示,不承载任何模型调用。
- **不做多账户聚合面板/团队共享**。单机单用户。
- **不做汇率换算**。各接口标注的 USD 实际结算为人民币,统一按 ¥ 展示并直接求和。
- **不做历史账单导出**(原型稿底栏曾有此入口,已确认不做)。
- **不做余额不足的"代申请/代充值"**。只展示状态,不代替用户执行外部动作。

---

## 2. 渠道模型

### 2.1 两类渠道,分开渲染

这是整个产品最基础的一条设计约束:**百分比和金额不能相加,所以两类渠道从数据模型到渲染都必须分开。**

| 类型 | `kind` | 有 `remaining/used/total` | 行内三格 | 进度条 | 参与底部消耗合计 |
|---|---|---|---|---|---|
| 金额型 | `amount` | 是 | 今日 / 本周 / 本月消耗 | 有分母才画 | 是 |
| 配额型 | `percent` | 否,只有已消耗百分比 | 5 小时 / 本周 / 本月 三个限流窗口 | 不画(三条迷你条已表达余量) | **否** |

### 2.2 当前四个渠道

| 渠道 | id | 接口 | 类型 | 换算 | 稳定性 |
|---|---|---|---|---|---|
| 4SAPI | `4sapi` | `GET https://4sapi.com/api/usage/token` | 金额型 · **申请制** | 500 000 积分 = ¥1 | 公开 |
| OpenCode Go | `opencode-go` | `GET https://opencode.ai/zen/go/v1/usage` | 配额型 | 只返回百分比 | **未公开接口**,失败静默降级 |
| DeepSeek | `deepseek` | `GET https://api.deepseek.com/user/balance` | 金额型 · 充值型 | 直接取 `total_balance` | 公开 |
| Hapi | `hapi` | `GET https://ai.yuchuantest.com/v1/usage` | 金额型 · 充值型 | 20 积分 = ¥1 | 公开 |

### 2.3 三种额度模型

同一套界面要容纳三种完全不同的"分母",这是口径设计的核心:

| 模型 | 渠道 | 分母 | 大数字下方的口径标注 | 进度条 |
|---|---|---|---|---|
| **限额型** | (无当前渠道,保留能力) | 接口给的 `total` | `剩 37%` | 画 |
| **申请制** | 4SAPI | **单次额度**(默认 ¥200,可配) | `剩 9%`,副标题 `本轮额度 ¥200 · 再等 6 天可申请` | 画 |
| **充值型** | DeepSeek / Hapi | **无分母** | 空(拿到多少就是可用多少) | **不画** |

> 设计理由:4SAPI 接口给的 `total_granted` 是**累计发放**(只增不减),拿它当分母会越算越低、没有决策价值(实测 ¥18.99 / ¥399.83 = "剩 5%")。所以申请制渠道的分母换成单次额度。

---

## 3. 信息架构

产品把界面分成**两个互不重叠的面**,这是「设置与管理」改版确立的核心结构:

```
┌─ 展示面(body 默认)──────────────────┐   ┌─ 配置面(body.view-manage)──────────┐
│  标题栏   计数 / 刷新 / 设置 / 置顶 /  │   │  ← 返回   设置与管理                │
│           切换紧凑条 / 隐藏           │   │                                     │
│  汇总条   今日 / 本周 / 本月消耗合计   │   │  密钥        每渠道:状态/删除/输入/保存│
│  渠道列表 行 = 图标+名称+状态点+主数值 │   │  额度申请    每渠道:开关+额度/间隔/日期│
│           +进度条+三格消耗             │   │  刷新        展开态/折叠态/退避间隔   │
│           └ 展开:区间/明细/迷你柱图     │   │  渠道顺序    ↑↓ 调自定义排序          │
│  底栏     状态汇总 / 设置与管理 / 退出 │   │  胶囊显示    多选芯片                 │
└──────────────────────────────────────┘   │  显示        排序/置顶/提醒/自动折叠   │
                                            │  阈值        标黄 / 标红              │
                                            │  启动        开机自启                 │
                                            └─────────────────────────────────────┘
```

**约束:展示区不放任何设置项。** 行详情里只留一句「密钥、刷新与阈值在『设置与管理』里配置」的入口链接。这条约束来自一次真实的设计失败:改版前设置是一个下压面板、密钥藏在每个渠道的展开区里,两处分散且"管理密钥"入口实际是展开第一行,名不副实。

**入口(4 个,覆盖所有路径)**:标题栏 `⚙` · 底栏「设置与管理」 · 空态引导按钮「去配置密钥」 · 行详情链接。出口:`←` 返回按钮 · `Esc`。

---

## 4. 形态系统

### 4.1 四种形态

| 形态 | body 类 | 尺寸(逻辑 px) | 进入方式 | 退出方式 | 可拖拽区 |
|---|---|---|---|---|---|
| **面板** | `form-panel` | 窗口 400 × 580(内容 380×560 + 四周 10px 透明边距) | 默认 / 从其它形态展开 | 切紧凑条 / 收成胶囊 / Esc | 标题栏 |
| **紧凑条** | `form-compact` | 窗口 内容宽+20 × 66(内容 ≤380×46) | 标题栏 `▬` | 条尾 `▲` 展开面板 / `●` 收成胶囊 | 整条(chip 区) |
| **胶囊** | `form-pill` | 窗口 内容宽+20 × 66(内容 132~280×46) | 紧凑条条尾 `●` | 胶囊右侧 `▲` 展开面板 | 整块 |
| **贴边** | `body.docked` | 8 × 64(**无边距**) | **把窗口拖到屏幕左/右边缘松手** | 把窗口从边缘拖走 | 整块 |

> 2026-09-23 起窗口四周留 10px 真透明边距,卡片画在内部并带柔和外阴影
> (`0 2px 8px -2px rgba(0,0,0,.6)`,外延 ≤10px 不被窗口边界裁切)。
> 吸附展开时窗口向贴边侧多探出一个边距,让卡片边缘与屏幕边缘贴齐。

### 4.2 关键设计决策

**① 所有形态固定尺寸,不可拖拽改变大小。**
`resizable:false` + `maximizable:false`,每次切换形态都 `setResizable(false)` 并把 min/max 钳到当前尺寸。
> 机制的真相(实测):Windows 对**程序化** `SetWindowPos` 不套用 tracking 约束,`setMinSize/setMaxSize` 调用会成功返回但拦不住 `setSize(500,600)`。真正挡住用户拖拽的是窗口样式里**没有 `WS_THICKFRAME`(缩放边框)和 `WS_MAXIMIZEBOX`**。`build/win-style.ps1` 读 `GWL_STYLE` 做这条断言(实测 `0x14ca0000`,两个位都不在)。min/max 保留作兜底,但不要再把它当成固定尺寸的机制。

**② 窗口 `transparent:true` + `shadow:false`,body 自己就是那张卡片。**
圆角外是真透明,不再有窗口底色露角。面板圆角 12 / 紧凑条 10 / 胶囊 999。

**③ 贴边形态刻意不显示任何数字。**
只留一条按状态色渐变的纯色竖条,把占屏面积压到 8×64。信息量让位给"不占地方"。展开后右边缘保持贴齐,窗口不会跑到屏幕外。

**④ 吸附判定写成「贴齐 `||` 探出」。**
真实拖动时窗口通常**探出屏幕外**(实测松手时右边缘在屏幕外 215px),只判"恰好贴齐"会漏掉绝大多数真实拖动。

**⑤ 拖动判定:停止移动 250ms 后判定,且自己的 `setPosition` 触发的 `moved` 事件要屏蔽 600ms。**
否则程序化的形态切换会被误判成用户在拖动。

### 4.3 形态与显示内容的关系

| 元素 | 面板 | 紧凑条 | 胶囊 | 贴边(收起) | 贴边(展开) |
|---|---|---|---|---|---|
| 标题栏 | ✓ | — | — | — | ✓ |
| 汇总条 | ✓ | — | — | — | ✓ |
| 渠道列表 | 竖排完整行 | 横排 chip | — | — | ✓ |
| 行内进度条 / 三格消耗 | ✓ | — | — | — | ✓ |
| 行展开详情 | ✓ | — | — | — | ✓ |
| 形态切换按钮 | 标题栏 6 个 | 条尾 2 个 | 右侧 1 个 | — | 同吸附前形态 |
| 底栏 | ✓ | — | — | — | ✓ |
| 状态色竖条 | — | — | — | ✓ | — |

---

## 5. 视觉规范

### 5.1 色板

```
底色层   --bg   #12141a   卡片底
        --bg2  #191c24   标题栏渐变起
        --bg3  #20242e   chip / 按钮 / 输入框底
线      --line #282d3a   主描边
        --line2 #333a4a  次级描边 / 滚动条
文字    --tx   #e6e9f0   主文字
        --tx2  #9aa3b5   次要文字
        --tx3  #646d80   标签 / 说明(⚠ 见 10.1-A4,对比度不足)
状态    --ok   #3ecf8e   正常(剩余 > 标黄线)
        --warn #f0b23c   预警
        --bad  #f0554d   危险 / 已限流
强调    --ac   #5b8cff   选中态 / 链接 hover / 主按钮
```

**状态色对比度实测**(相对 `--bg #12141a`,WCAG 计算):`--ok` 9.2:1 · `--warn` 9.8:1 · `--bad` 5.4:1 · `--tx3` **3.5:1(不达标)**。

### 5.2 字号阶梯(现状)

| 用途 | 字号 | 位置 |
|---|---|---|
| 渠道名 | 13 | 列表行 |
| 主数值 | 20 | 列表行(金额/百分比) |
| 胶囊主数值 | 14 | 胶囊 |
| 汇总数值 | 13.5 | 汇总条 |
| 底栏 / 紧凑条 chip | 11 / 12 | 底栏 / chip |
| 副标题 / 口径 | 11 | 列表行 |
| 三格标签 / 数值 | 11 / 12 | 行内消耗 |
| 三窗口标签 / 数值 | 11 / 11.5 | OpenCode 行 |
| 汇总标签 / 说明 | 10.5 | 汇总条 |
| 管理页字段 | 12 | 管理页 |

> 现状共 **9 档**(10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 14 / 20),相邻差 0.5px 在 100% 缩放下不可分辨。收敛方案见 10.1-A5。

### 5.3 圆角与间距

| 项 | 值 |
|---|---|
| 面板圆角 | 12 |
| 紧凑条圆角 | 10 |
| 胶囊圆角 | 999 |
| 贴边竖条圆角 | 4(左侧吸附 4/0/0/4,右侧镜像) |
| 图标容器圆角 | 28px 容器 → 8;24px → 7;16px → 4 |
| 行内边距 | `9px 12px 8px`(放大字号后从 `10px 12px 9px` 收紧 2px,保证四渠道一屏) |
| 卡片描边 | `1px solid --line` + `0 0 0 1px #ffffff08 inset` 内高光 |

### 5.4 图标规范

统一入口 `iconHtml(c, cls, off)` —— 列表行 / 密钥行 / 额度申请行 / 紧凑条 chip 共用。

| 渠道 | 素材 | 说明 |
|---|---|---|
| OpenCode Go | `logos/opencode.svg`(612 B) | 官方标,自带 `#131010` 深色底,在深色界面呈现为白色方框 |
| DeepSeek | `logos/deepseek.png`(6.4 KB) | 官方标,品牌蓝 `#4d6bfe` 透明底,直接放在界面底色上(**不要**再加渠道色方块,同色叠加会看不见) |
| 4SAPI / Hapi | CSS 字母方块 | `4S` / `HA`,底色取渠道主题色 |

**`off` 态(未配置或取数失败)**:字母方块变灰 `#333a4a`,官方标 `grayscale + 50% 透明`。两边语义一致。

素材本地内嵌(前端 CSP 只允许 `'self'` 与 `data:`),出处与版权说明见 `app/src/logos/README.md`。

### 5.5 动效

> 2026-09-23 第二轮落地(`design-ui-motion-2026-09-23.html`,原 `design-motion.html` 已并入):全部纯 CSS/合成器动画,时长 ≤320ms,曲线统一 `cubic-bezier(.2,.8,.2,1)`,永不回弹;`prefers-reduced-motion` 一键全静。

| 场景 | 实现 |
|---|---|
| 数值滚动 | 值真的变了才播:新值下方 9px 淡入、旧值幽灵层上移淡出,320ms(面板行与胶囊共用 `rollValue`) |
| 刷新反馈 | 取数期间「刷新」按钮持续脉冲(禁用),完成后才停;有变化的行扫一道 6% 微光(560ms,60ms 错峰);结束弹 toast「已更新 · 刚刚 / N 个渠道取数失败」 |
| 行展开 | `.rbody` `grid-template-rows:0fr→1fr`,展开 320ms / 收起 200ms,箭头转 90° |
| 形态切换 | D1 内容淡换:窗口尺寸一步到位,新内容 200ms 淡入上浮(D2 窗口形变已否决:每帧一次 setSize IPC) |
| 胶囊轮播 | 8 秒一换,200ms 纵向翻页 + 图标淡入;底部 3px 指示点(当前页高亮);悬停不暂停但节奏 ×3 |
| 面板入场 | 行自上而下 30ms 错峰淡入,只在进入面板/托盘唤回时放一次 |
| 贴边展开 | 内容从贴边侧滑入 + 淡入 180ms(`dock-anim`);拖拽近边缘 40px 内显示 2px 吸附预览线 |
| 跌破红线 | 状态点播一次 1.2s 红晕(只播一次,持续低位不重复);另有无限制呼吸循环,由 `alertPulse` 开关控制,默认关 |
| 进度条宽度 | `transition:width .5s cubic-bezier(.3,1,.4,1)` |
| 管理页切换 | 展示面 ↔ 配置面 120ms 淡入 |

---

## 6. 交互规范

### 6.1 全局

| 交互 | 行为 |
|---|---|
| 拖动窗口 | 面板走标题栏;紧凑条 / 胶囊 / 贴边竖条整块可拖 |
| 拖到屏幕边缘 | 停止移动 250ms 后判定,距边缘 16 逻辑像素内(或已探出屏外)即吸附到该侧;左右都支持,左侧镜像圆角 |
| 从边缘拖走 | 解除吸附,留在松手位置 |
| `Esc` | 管理页 → 关闭管理页;面板 → 收成胶囊;其它 → 展开为面板 |
| 托盘左键 | 切换窗口显示/隐藏 |
| 托盘右键 | 菜单:显示面板 / 立即刷新 / 退出 |

### 6.2 面板

| 交互 | 行为 |
|---|---|
| 点行头 | 展开该行详情(单开,自动收起其它行);再点收起 |
| 区间按钮 | 近 24 小时 / 近 7 天 / 近 30 天,切换后拉取迷你柱图数据 |
| 标题栏 `⟳ 刷新` | 立即刷新(取数期间按钮持续脉冲禁用,完成后 toast 回执) |
| 标题栏排序指示(`↓ 剩余`) | 点击循环切换 剩余/余额/消耗/自定义 四种排序,与管理页下拉双向同步 |
| 标题栏 `⚙ 设置` | 打开「设置与管理」 |
| 标题栏 `📌` | 切换全局置顶(窗口状态与配置一起改,选中态实心强调色) |
| 标题栏 `▬` | 切紧凑条 |
| 标题栏 `✕` | 隐藏到托盘 |
| 底栏「设置与管理」/「退出」 | 打开管理页 / 退出应用 |

### 6.3 紧凑条

- chip = 「状态点 + 数值」。**不放渠道名**(图标已带缩写),金额舍掉小数位而百分比保留 `%`。
- 16px 档位的图标统一回退为**字母方块**(U6:官方位图缩到 14px 会糊,字母更可辨识);面板/管理页 28px 档继续用官方标。
- 按重要度排在前面,尾部放不下的收进 `+N` 徽标。**`+N` 可交互**:悬停列出被藏渠道名;点击展开面板、滚动到第一个被藏渠道并高亮 1 秒。
- 条尾两个按钮:`▲` 展开为面板、`●` 收成胶囊。

### 6.4 胶囊

- 显示哪个渠道可在管理页配置:**一个都不选 = 自动显示最紧张的一个**;选 1 个 = 固定;选多个 = 每 8 秒轮播。
- **不做悬停暂停**,但悬停时轮播节奏 ×3(指示点让"会变"可见,不需要暂停;指针常停在胶囊附近,一停就像轮播坏了)。底部 3px 指示点标出轮播位置。
- 右侧常驻 `▲` 展开为面板。悬停显示完整渠道名 + 剩余百分比 + 轮播位置。

### 6.5 贴边

| 交互 | 行为 |
|---|---|
| 拖拽靠近边缘 | 进入 40 逻辑像素内,窗口贴边侧显示 2px 吸附预览线(吸附可发现) |
| 移入竖条 | 延迟 **150ms** 展开为吸附前的形态(8px 太窄,不加意图延迟鼠标路过就会弹开);展开内容从贴边侧滑入 + 淡入 180ms |
| 移出窗口 | **1 秒**后收回(判定只认文档级 `mouseleave`) |
| 显式收回 | 展开态右下角 `✕` 按钮,或 `Esc` —— 收回为竖条,不退出吸附 |
| 在管理页里操作 | 不收回 |
| 吸附期间 | 强制置顶(否则鼠标移过去也看不见它) |

> **折叠判定的坑(已修)**:不能用 `body.matches(":hover")` 当信号。窗口会周期性重绘(轮询 render 替换 DOM),之后若没有任何鼠标事件,Chromium 不会重算 hover —— 那份"陈旧 false"会让光标明明还在窗口里也把窗口收走(实测:光标停在窗口内,每秒采样 hover 为 true→false→false→false…)。折叠**只认文档级 `mouseleave`**。

### 6.6 设置与管理

| 分组 | 内容 |
|---|---|
| **密钥** | 每渠道一行:图标 + 名称 + 状态点 + 删除按钮;下行:密码输入框 + 保存。只写入 Windows 凭据管理器,界面明文输入、保存后不回显 |
| **额度申请** | 只列金额型渠道。每渠道一个开关,开启后展开 单次额度 / 最短间隔 / 上次申请日期,以及「记一次申请=今天」「清除手动值」 |
| **刷新** | 面板展开时 30s / 1min / 2min / 5min;折叠托盘时 1 / 5 / 10 / 30min;连续失败后 5 / 15 / 30min |
| **渠道顺序** | 每行渠道 + `▲` `▼`,改完立即保存并重排(排序是前端算的,不需重新取数) |
| **胶囊显示** | 每个渠道一个多选芯片 |
| **显示** | 渠道排序(剩余百分比升序 / 余额 / 今日消耗 / 自定义顺序)、窗口置顶、余额不足时提醒、失焦后自动折叠 |
| **阈值** | 标黄 50/40/30/20%;标红 20/15/10/5% |
| **启动** | 开机自启(静默进托盘) |

---

## 7. 状态与数据语义

### 7.1 状态色阈值

```
剩余 > 标黄线(默认 40%)   → 绿  --ok
标红线(15%) < 剩余 ≤ 40%  → 黄  --warn
剩余 ≤ 标红线(默认 15%)   → 红  --bad
未配置密钥                 → 空心灰环  .dot.off(没启用,不是故障)
取数失败 / 失联            → 斜杠灰点  .dot.st(故障,需要被看见)
```

> 2026-09-23 起灰色拆成两种形状(U2):未配置与失联不再共用一个灰点,形状区分兼顾色盲可用性。列表行 / 胶囊 / 渠道卡 / 底栏共用 `dotCls()`。跌破红线的瞬间状态点播一次 1.2s 红晕(M7,只播一次)。

配额型渠道的比例取**本月窗**剩余(订阅按自然月结,5 小时/本周的波动不该左右整行观感);金额型取 `remaining / 分母`,分母 = 申请制单次额度,否则接口 `total`;充值型无分母 → 返回 `null`,不画进度条、不参与百分比排序、状态恒为绿。

### 7.2 取数失败:绝不降级成 0

余额类工具最危险的 bug 是把"请求失败"渲染成"余额归零",用户会以为欠费。所以:

- 取数失败时**沿用上次成功快照** + 标注时间 + 显示真实错误原因;
- `remaining` 取不到时一律返回 `null` 而非 `0`;
- 行副标题显示「取数失败」,口径位显示「上次快照」或「失联」;
- 行内提示「显示的是 N 分钟前的成功快照 · <错误原因>」。

### 7.3 消耗是推算值

DeepSeek / Hapi 的接口只给当前余额、不给累计消耗,日/周/月消耗靠本地 SQLite 快照差值算:

```
consumption(t0,t1) = Σ max(0, remaining[i-1] - remaining[i])
```

- 负差值(充值、配额重置)clamp 成 0;
- **窗口起点没有基线时返回 `null` 而不是 0**,界面显示「—— / 数据不足」;
- 程序未运行的时段形成数据空洞,数字偏小,界面已标注"推算";
- 今日按**本地零点**起算,本周按**周一**起算(不按 UTC,否则跨时区会指错日子)。

**环比(U8,2026-09-23 落地)**:汇总条每格同时给出与上一周期**同时段**的对比
(今日 vs 昨日同时段、本周 vs 上周同时段、本月 vs 上月同时段,后端字段
`dayPrev / weekPrev / monthPrev`)。拿整段上周期比会系统性偏大,所以必须是"同时段"。
只在"本期与上期都有数"的渠道集合上算,上期合计为 0 或集合为空 → 显示「——」。
费用语境:消耗涨 = 红 ↑,降 = 绿 ↓。

### 7.4 申请制额度(4SAPI)

| 项 | 规则 |
|---|---|
| 分母 | 单次额度(默认 ¥200,可配) |
| 上次申请时间 | **自动检测**:每次轮询写快照前比对可用余额,跳升 ≥ ¥20 记为一次申请(回溯 90 天,与快照保留期对齐) |
| 手动修正 | 优先于自动值;**除非**检测到比"手动写入时刻"更晚的跳升(说明又申请了一次,自动值接管) |
| 倒计时 | 按整天**向下取整**(宁可保守,也不能提前说"可申请"——用户去申请会被拒) |
| 撑不到预警 | 近 7 天日均消耗(数据覆盖不足 1 天不估算)→ 预计可用天数 < 距可申请天数时行内黄字提示 |
| 阈值 | 低于 ¥20 的抖动不算申请 —— 接口金额有四舍五入,不设阈值会被误判 |

### 7.5 轮询

四个接口都是账务/元数据接口,**查询余额不产生任何 token 计费**。所以间隔可以压得比较短,唯一约束是对方的限流礼貌性:

```
面板展开 60s  /  折叠、托盘 300s  /  连续失败 3 次后 900s  /  带 0~4s 抖动
```

### 7.6 安全

- 密钥只写 Windows 凭据管理器(服务名 `TokenScope`,账户名 = 渠道 id);
- 密钥**永不**进 JSON / SQLite / 日志 / 导出文件;配置可随便备份分享;
- 出网请求只发往渠道自身域名,无遥测、无云同步;
- `.gitignore` 排除 `*.db`(快照含余额与用量历史)。

---

## 8. 已开发功能清单

### 8.1 逻辑层 `app/core`

| # | 功能 | 状态 | 落点 |
|---|---|---|---|
| 1 | 四渠道响应解析与单位换算 | ✅ | `providers.rs` |
| 2 | 渠道静态定义表(`PROVIDERS`)+ 加新渠道的扩展点 | ✅ | `providers.rs` |
| 3 | SQLite 快照库(WAL、索引、90 天清理) | ✅ | `store.rs` |
| 4 | 消耗推算(相邻快照差值 + 负值 clamp + 无基线返回 `None`) | ✅ | `store.rs` |
| 5 | 窗口内消耗合计(不要求窗口起点有基线,供日均推算用) | ✅ | `store.rs` |
| 6 | 余额跳升检测(申请/充值识别,带最小阈值) | ✅ | `store.rs` |
| 7 | 迷你柱图序列(按小时分桶) | ✅ | `store.rs` |
| 8 | Windows 凭据管理器封装(读/写/删/存在性/尾号掩码) | ✅ | `secrets.rs` |
| 9 | 取数、失败降级、视图组装 | ✅ | `fetch.rs` |
| 10 | 申请制额度状态机(倒计时/手动优先/撑不到预警) | ✅ | `fetch.rs` |
| 11 | 本地零点 / 周一起算 / 月初起算 | ✅ | `fetch.rs` |
| 12 | 设置项(`Config`)+ 容器级 `serde(default)` 老库兼容 | ✅ | `config.rs` |
| 13 | 26 个单元测试 | ✅ | 各模块 `#[cfg(test)]` |

### 8.2 外壳 `app/src-tauri`

| # | 功能 | 状态 | 落点 |
|---|---|---|---|
| 14 | Tauri 2 外壳与命令注册(11 个命令) | ✅ | `src/lib.rs` |
| 15 | 后台轮询调度(形态感知间隔 + 失败退避 + 抖动) | ✅ | `poll_loop` |
| 16 | 托盘:左键显隐、右键菜单(显示面板/立即刷新/退出) | ✅ | `setup` |
| 17 | 开机自启(HKCU Run,每次启动按 `current_exe()` 重写路径) | ✅ | `autostart` |
| 18 | 全局置顶唯一写入口(窗口状态与配置一起改) | ✅ | `set_pin` |
| 19 | ACL 权限集(形态切换 / 贴边移动 / 拖拽所需) | ✅ | `capabilities/default.json` |
| 20 | 固定尺寸窗口样式(无 `WS_THICKFRAME` / `WS_MAXIMIZEBOX`) | ✅ | `tauri.conf.json` + `win-style.ps1` 断言 |

### 8.3 前端 `app/src`

| # | 功能 | 状态 | 落点 |
|---|---|---|---|
| 21 | 四形态渲染与切换(固定尺寸) | ✅ | `app.js` `applyForm` / `styles.css` |
| 22 | 拖到屏幕边缘自动吸附(左右侧、探出判定、拖离解除) | ✅ | `scheduleSettleCheck` / `dockEnter` / `dockExit` |
| 23 | 贴边悬停展开 + 移出收回(150ms 意图延迟 / 1s 收回) | ✅ | `dockLayout` / `dockScheduleCollapse` |
| 24 | 渠道行:状态点 + 主数值 + 副标题 + 口径 + 进度条 + 三格消耗 | ✅ | `renderRow` |
| 25 | 配额型三窗口明细(标签 + 迷你条 + 剩余%,按阈值染色) | ✅ | `renderRow` `.wins` |
| 26 | 行展开详情(区间切换 / KV 明细 / 迷你柱图 / 数据来源说明) | ✅ | `renderDetail` |
| 27 | 消耗汇总条(仅金额型,口径与合计) | ✅ | `renderSummary` |
| 28 | 紧凑条 chip + `+N` 截断 + 条尾切换按钮 | ✅ | `fitCompact` |
| 29 | 胶囊:自动最紧 / 固定 / 多选轮播 + 悬停提示 | ✅ | `renderPill` / `renderPillFace` |
| 30 | 贴边竖条染色(与胶囊同一套选择逻辑) | ✅ | `renderDock` |
| 31 | 「设置与管理」独立视图(8 个分组) | ✅ | `openManage` / `renderKeyRows` 等 |
| 32 | 密钥保存 / 删除(带回滚与失败提示) | ✅ | `manage` 事件委托 |
| 33 | 申请制额度配置(开关 / 额度 / 间隔 / 日期 / 记一次 / 清除) | ✅ | `renderClaimRows` |
| 34 | 渠道自定义排序(`▲▼` 立即生效并落库) | ✅ | `renderOrderRows` |
| 35 | 胶囊显示渠道多选 | ✅ | `renderPillPick` |
| 36 | 排序四模式(剩余百分比 / 余额 / 今日消耗 / 自定义) | ✅ | `sortChannels` |
| 37 | 隐藏未配置渠道 + 空态引导卡 | ✅ | `render` |
| 38 | 官方图标 + 字母方块回退 + `off` 态去色 | ✅ | `iconHtml` |
| 39 | 置顶按钮与设置项双向同步 | ✅ | `applyPinUI` |
| 40 | `Esc` 分级回退 | ✅ | `keydown` |

### 8.4 工具链 `build/`

| # | 功能 | 状态 |
|---|---|---|
| 41 | 免管理员工具链安装(Rust GNU + MinGW) | ✅ |
| 42 | 构建入口(check / test / dev / build / bundle) | ✅ |
| 43 | 图标生成(手写 PNG/ICO,无依赖) | ✅ |
| 44 | 三形态端到端验证(`verify-form.js`,含 Win32 样式位断言) | ✅ |
| 45 | CDP 实时 DOM 探针 + 调试启动脚本 | ✅ |
| 46 | 密钥泄漏扫描 / 推送结果验证 | ✅ |

### 8.5 未实现 / 部分实现

| 项 | 状态 | 说明 |
|---|---|---|
| 余额低于阈值的系统通知 | ❌ 开关已存在但未接线 | 见 10.2-B10 |
| 失焦后自动折叠 | ❌ 开关已存在但未接线 | 原型稿写的是"失焦后 3 秒" |
| 托盘图标角标(最紧张渠道百分比) | ❌ 当前是静态图标 | 见 10.2-B10 |
| 全局快捷键(`Alt+T` 唤起) | ❌ | 原型稿交互说明里的承诺 |
| 密钥尾号回显 | ⚠️ 后端命令已实现未接线 | `key_hint` 死代码 |
| 窗口位置持久化 | ❌ 仅形态持久化 | 见 10.2-B13 |
| 点击穿透 / 锁定模式 | ❌ | 见 10.2-B5 |
| NSIS 安装包 | ⚠️ 未跑过 | `build.ps1 bundle` |
| 单 exe 交付 | ⚠️ 阻塞 | GNU 工具链下 exe 加载期依赖 `WebView2Loader.dll`,三个方案待定夺(见 `PROGRESS.md`) |

---

## 9. 设计稿差异台账

原型稿 `token-dashboard-prototype.html` 是视觉基准,但实现过程中有多处**有意偏离**。记录在此,避免后续把"有意为之"当成 bug 修掉:

| # | 原型稿 | 实现 | 原因 |
|---|---|---|---|
| 1 | 吸附态是 26px 竖标签,**竖排文字显示渠道与百分比** | 8×64 **纯色竖条,不显示任何数字** | 用户要求"尽量少占屏幕";信息量让位给面积 |
| 2 | 吸附由按钮触发 | **拖到屏幕边缘自动吸附**,按钮已删除 | 减少界面元素;三种形态都能拖 |
| 3 | 设置是内嵌下拉面板(`.pane`) | **独立视图** `body.view-manage` | 设置与展示必须分开,原方案两处分散且入口名不副实 |
| 4 | 汇总条带环比箭头 `↑12% / ↓4%` | 去掉,改为「推算 / N 个渠道」 | 环比需要历史同期数据,当时未实现(建议恢复,见 10.2-B7) |
| 5 | 行详情有「打开控制台 / 用量趋势 / 设置阈值 / 重试」按钮 | 收敛为一句「设置与管理」链接 | 展示区不放操作项 |
| 6 | 胶囊"多平台并列时鼠标滚轮可轮播" | **5 秒定时轮播**,可配置显示哪些渠道 | 滚轮与桌面挂件的交互冲突;轮播可见性更好 |
| 7 | 自定义排序是"拖拽排序" | `▲▼` 按钮排序 | 拖拽排序在 380px 宽的行里手势冲突严重 |
| 8 | 主数值的小数位 / 百分号缩小变灰(`.val .v i{font-size:12px;color:var(--tx3)}`) | 统一一套样式(`font-size:inherit`) | 只有整数部分"有样式",看起来像半截数字没渲染好 |
| 9 | 选中态是柔和强调底色(`.ib.on{color:var(--ac);background:#5b8cff1f}`) | **实心强调色**(`background:var(--ac)` + 白图标) | 置顶图标从 emoji 📌 改为 `currentColor` 的 SVG —— emoji 自带颜色,CSS `color` 对它无效,选中/未选中只差 12% 透明度的淡蓝底,看不出来 |
| 10 | 卡片有外阴影(`box-shadow:0 22px 60px -12px #000`) | **去掉**(`shadow:false`,无外阴影) | 窗口 `transparent:true`,圆角外是真透明;外阴影会被窗口边界裁掉(建议恢复,见 10.1-A6) |
| 11 | 6 个平台(含 OpenCode Go 备用号) | 4 个 | 按实际使用收敛 |
| 12 | 底栏有「导出账单」 | 去掉 | 非目标 |
| 13 | 主数值 19px / 渠道名 12px / 三格 10px | 20px / 13px / 11px(整体 +1px) | 用户反馈字号偏小 |
| 14 | OpenCode 大数字取"最紧窗口" | 固定取**本月窗** | 订阅按自然月结,5 小时/本周的波动不该左右整行观感 |
| 15 | 充值型显示"充值余额 · 无限额" | 去掉说明文案 | 拿到多少就是可用多少,不需要解释 |
| 16 | 4SAPI 分母 = 接口 `total_granted`(累计发放) | 分母 = **单次额度 ¥200** | 累计发放只增不减,拿它当分母会越算越低,没有决策价值 |

---

## 10. 优化建议

> **2026-09-23 第二轮落地**(设计稿 `design-ui-motion-2026-09-23.html`,本轮实测代码状态后按差集实施):
> ✅ 已落地 —— A3/U2 状态点分层 · A7/U5 滚动条(此前已覆盖 `.extab`)· A9 动效规范(v0.1.13 动效层)· A10 reduced-motion(已有)· A11/U7 焦点态 + 开关 `<button role="switch">` · B3/I6 贴边显式收回(✕+Esc)· B6/M2 刷新真实进度 + toast · B7/U8 汇总环比(后端 `dayPrev/weekPrev/monthPrev`)· B8/I1 排序前置 · B9/I2 `+N` 可交互 · B12/I3 行内确认 + toast 替代 confirm/alert · B13/I4 位置持久化(O-6 已落地)· B14 管理页过渡 · B15/I5 保存成功高亮+尾号 · B1 吸附预览线 · B4 轮播指示点+8s+悬停×3 · A6/U4 卡片外阴影(10px 边距,阴影外延按 ≤10px 重定为 `0 2px 8px -2px`)· A8/U6 紧凑条字母方块 · M7 跌破一次性呼吸(替代 G 无限循环的默认形态)。
> ⏳ 仍未做 —— A1 设计令牌(部分:动效/颜色已是变量)· A2 浅色主题(用户裁决:只做深色)· A5 字号收敛(`max(11.5px)` 下限已事实收敛)· B2 吸附轻量预览行(悬停仍直接展开完整形态)· B5 点击穿透 · B10 通知接线 · B11 行展开多开 · B16 形态快捷键 1/2/3(全局热键 Alt+G 类已有)· B17 托盘菜单补全。

按「影响面 × 实施成本」分三级。**P0 = 建议优先做**,多数是一行到几行的改动但用户能直接感知;P1 = 显著提升质感与效率;P2 = 打磨与合规。

### 10.1 视觉与样式

#### A1 · 建立设计令牌层 `P1`

**现状**:只有颜色是 CSS 变量。字号 9 档散落在 `styles.css` 与 `app.js` 的内联样式里;状态色靠 JS 里的 `TONE_HEX` 拼内联 `style` 属性(如 `style="color:${TONE_COLOR[tone]}"`)。
**问题**:改一次视觉要同时动 CSS 和 JS;无法主题化;状态色散在 JS 常量与 CSS 变量两处,容易走偏。
**方案**:补齐 `--fs-*`(5 档)/ `--r-*`(4 档)/ `--sp-*`(6 档)/ `--dur-*`(3 档)/ `--ease-*`(2 档);状态色改为语义类 `.t-ok / .t-warn / .t-bad / .t-off`,JS 只负责设置类名。
**收益**:改版成本从"改 30 处"降到"改 1 处";为主题化铺路。

#### A2 · 浅色主题 `P1`

**现状**:仅深色。`--bg #12141a` 卡片常驻桌面。
**问题**:白天浅色壁纸下对比过强;部分用户强烈偏好浅色挂件。
**方案**:`:root[data-theme="light"]` 覆盖变量;管理页「显示」加三档切换(跟随系统 / 浅色 / 深色),默认 `prefers-color-scheme`。窗口 `transparent:true` 已具备条件,只需换 body 底色与描边。
**收益**:挂件"融入桌面"的能力大幅增强;这是同类产品的高频诉求。

#### A3 · 状态点语义分层 `P0`

**现状**:`.dot.o`(灰点)**同时表示"未配置密钥"和"取数失败"**。
**问题**:两者语义完全不同(一个是没启用,一个是故障),用户无法一眼区分,而这正是最需要区分的两种状态。
**方案**:拆成 `.dot.off`(空心灰环 = 未配置)与 `.dot.stale`(灰 + 感叹/斜线 = 失联),并补形状区分以满足色盲可用性。
**收益**:状态可辨;同时是可达性改进。

#### A4 · 文字对比度达标 `P0`

**现状**:`--tx3:#646d80` 被大量用于 10.5–11px 的标签与说明文字(汇总条标签、副标题、三格标签、底栏、`hint`)。
**问题**:实测在 `--bg #12141a` 上的对比度为 **3.54:1**,低于 WCAG AA 对小字的 4.5:1。而它承载的恰恰是最小最密的字。
**方案**:`--tx3` 提到 `#7b8499`,实测对比度 **4.91:1** 达标。如需保留层级差,可只把"标签/说明"提到新值,纯装饰文字保留原值。
**收益**:小字可读性显著改善,在 4K / 高 DPI 屏上尤其明显。**这是投入产出比最高的一条。**

#### A5 · 字号阶梯收敛 `P1`

**现状**:9 档(10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 14 / 20)。
**问题**:相邻差 0.5px 在 100% 缩放下不可分辨,却让"哪个用哪档"变成需要记忆的规则。
**方案**:收敛到 **11 / 12 / 13 / 15 / 20** 五档(标签 11、正文 12、强调 13、小标题 15、主数值 20),配套行高 1.5 / 1.45 / 1.4 / 1.35 / 1.1。
**收益**:视觉更整齐,改版可预期;配合 A1 一次性落地。

#### A6 · 恢复卡片外阴影(留透明边距)`P1`

**现状**:窗口 `transparent:true` + `shadow:false`,圆角外真透明,但卡片与桌面之间**没有层次**。
**问题**:浅色壁纸上卡片"浮不起来",边缘与背景混在一起。原型稿本来有 `0 22px 60px -12px #000` 的外阴影,实现时因窗口边界裁切而丢失。
**方案**:窗口四周留 **10px 透明边距**,卡片画在内部并加 `box-shadow:0 18px 48px -14px rgba(0,0,0,.55)`;各形态尺寸按"卡片尺寸 + 边距"重新标定(面板 400×580 / 紧凑条 400×66 / 胶囊 220×66 / 贴边 8×64 不变)。
**注意**:会改变现有固定尺寸的标定值,需同步 `build/verify-form.js` 的期望值与 `win-style.ps1` 的断言。
**收益**:立体感对齐设计稿;贴边态与桌面边缘的关系也更自然。

#### A7 · 滚动条样式覆盖所有可滚动容器 `P0`

**现状**:`.list` 与 `.mbody` 有自定义滚动条,但 `.rbody`(行详情,`overflow-y:auto`)没有。
**问题**:行详情内容超长时出现**系统浅色滚动条**,在深色卡片里非常突兀 —— 这是"一眼可见"的瑕疵。
**方案**:抽出统一的滚动条规则(`::-webkit-scrollbar` + `thumb`)应用到所有可滚动容器。
**收益**:视觉一致性;一行改动。

#### A8 · 小尺寸图标清晰度 `P1`

**现状**:DeepSeek 用 225×225 PNG 转出的位图;紧凑条容器 16×16,官方标内缩到 88%(约 14px)。
**问题**:位图在 14–16px 下重采样,鲸鱼细节糊成一团(OpenCode 是 SVG 不受影响)。
**方案**:DeepSeek 改用官方 SVG;或提供 16 / 24 / 32 三档预渲染位图。紧凑条这一档可回退为字母方块(小尺寸下字母比图形更可辨识)。
**收益**:紧凑条那一排 chip 的图标不再是"糊点"。

#### A9 · 动效规范统一 `P1`

**现状**:进度条 `cubic-bezier(.3,1,.4,1)`,行展开 `ease`,刷新旋转 `ease`,贴边展开**无过渡**。
**问题**:节奏不一致;**贴边展开是瞬变**,在屏幕边缘突然弹出 380×560 很突兀。
**方案**:定义 `--dur-1:120ms`(状态反馈)/ `--dur-2:240ms`(展开收起)/ `--dur-3:320ms`(形态切换);缓动统一 `--ease-out:cubic-bezier(.2,.8,.2,1)`;贴边展开加 180ms 的"从边缘滑出 + 淡入"。
**收益**:整体质感提升;贴边展开不再"砸"出来。

#### A10 · `prefers-reduced-motion` 支持 `P2`

**现状**:所有动画无条件播放。
**方案**:`@media (prefers-reduced-motion:reduce)` 下把时长压到 0(保留状态变化,去掉位移/缩放)。
**收益**:对前庭敏感用户友好;低成本合规项。

#### A11 · 焦点态与开关语义 `P0`

**现状**:`.sw`(开关)、`.pick`(芯片)、`.btn`、`.ib` 都没有 `:focus-visible` 样式;`.sw` 本身是 `<div>`,键盘完全不可达。
**问题**:键盘用户看不出焦点位置;开关无法用键盘操作。
**方案**:统一 `:focus-visible{outline:2px solid var(--ac);outline-offset:2px}`;`.sw` 改为 `<button role="switch" aria-checked="true|false">`;`.dot` 状态点补 `aria-label`。
**收益**:键盘可达 + 可访问性合规。

#### A12 · 紧凑条 chip 的信息完整性 `P2`

**现状**:chip 只有「图标 + 状态点 + 数值」,无单位后缀、无 `title`。
**问题**:金额 chip 显示 `187`,用户不知道是 ¥187 还是 187 积分;也无法悬停看渠道全名。
**方案**:数值保留单位(`¥187` / `1.3%`);补 `title`(渠道全名 + 剩余 + 更新时间)。
**收益**:紧凑条信息自解释,不用展开面板确认。

### 10.2 交互

#### B1 · 吸附预览线(可发现性)`P1`

**现状**:拖到边缘松手即吸附,但**界面上没有任何提示**(吸附按钮已被刻意移除)。
**问题**:新用户不可能发现这个功能;老用户也不知道"离多近才算"(阈值 16 逻辑像素不可见)。
**方案**:拖拽中当窗口边缘进入阈值(如 40px)时,在窗口贴边侧画一条 2px 的高亮预览线(完整版可在屏幕对应边缘绘制独立覆盖条)。
**收益**:功能可发现;吸附判定从"玄学"变成"看得见"。

#### B2 · 吸附展开改为「点击展开 + 悬停预览」`P1`

**现状**:鼠标移入 8px 竖条 150ms 后展开为完整形态(最大 380×560),且是瞬变。
**问题**:在屏幕边缘弹出一大块,鼠标路过就遮挡;且无法区分"路过"与"要看"。
**方案**:①悬停 150ms → 展开为**轻量预览**(仅最紧张渠道的一行,约 240×64);②**点击**才展开为完整形态;③展开动画从边缘滑出。
**收益**:误触大幅减少;信息获取分级,符合"不打扰"的产品价值。

#### B3 · 吸附展开态需要显式关闭入口 `P0`

**现状**:收回只能靠鼠标移出窗口 1 秒;若在设置页里操作则**不收回**。
**问题**:用户在吸附展开态进入设置页后,窗口会一直保持展开,没有任何"收回去"的按钮,只能靠把鼠标移出窗口。
**方案**:展开态加 `✕`(收回为竖条,不退出吸附);`Esc` 也收回。
**收益**:状态可预期,不会卡在展开态。

#### B4 · 胶囊轮播的节奏与可见性 `P1`

**现状**:5 秒切换,悬停不暂停(曾做过暂停,因"看起来像轮播坏了"被移除)。
**问题**:用户正看某个数时被切走;轮播这件事本身不可见,所以"不暂停"又带来了新的不确定感。
**方案**:①加**轮播指示点**(底部 3 个 3px 圆点,当前高亮)—— 让"会变"这件事可见,从而不需要暂停;②间隔放宽到 8–10 秒;③鼠标移入时把间隔**延长到 3 倍**而非暂停。
**收益**:同时满足"不被误认为坏"和"不打断阅读",比"暂停/不暂停"的二选一更好。

#### B5 · 置顶 + 点击穿透(锁定)模式 `P1`

**现状**:置顶开启后,挂件会挡住下层窗口的鼠标操作。
**问题**:桌面挂件最常见的抱怨就是"挡鼠标";常驻置顶把这个矛盾放到最大。
**方案**:加"锁定/穿透"开关(`set_ignore_cursor_events(true)`),开启后鼠标完全穿透,只能从托盘操作。吸附态需要悬停展开,所以只在面板 / 紧凑条 / 胶囊形态提供。
**收益**:解决常驻挂件的核心痛点。

#### B6 · 刷新按钮的真实进度反馈 `P0`

**现状**:点击后按钮旋转 720ms 动画即结束,但取数是 4 个渠道串行请求(单渠道超时 20s)。
**问题**:动画结束 ≠ 数据已更新,用户会重复点击;慢网下尤其明显。
**方案**:`refresh()` 期间把按钮置为 loading(禁用 + 持续旋转),完成后恢复,并在底栏短暂显示「已更新 · 刚刚」;失败时底栏显示失败渠道数。
**收益**:反馈与真实状态一致,减少重复点击。

#### B7 · 汇总条恢复环比 `P0`

**现状**:原型稿的汇总条带 `↑12% / ↓4%` 环比箭头,实现时被去掉,改为「推算 / N 个渠道」。
**问题**:只有绝对值,看不出"今天比昨天多花还是少花"—— 而这正是余额监控最想知道的信息。
**方案**:恢复环比:今日 vs 昨日同期、本周 vs 上周同期、本月 vs 上月同期;**数据不足时显示「——」而非 0**(与现有"取不到给 null"契约一致)。
**收益**:汇总条从"数字展示"升级为"趋势判断"。

#### B8 · 排序入口前置到展示区 `P0`

**现状**:排序只能在管理页的 `<select>` 里改。
**问题**:排序是高频操作(想看余额最高的 / 今天花得最多的),却藏在设置里,需要 3 步才能改。
**方案**:标题栏计数旁加一个可点击的排序指示(如 `↓ 剩余`),点击循环切换 4 种模式,悬停显示完整说明。
**收益**:高频操作一步可达;与"展示区不放设置项"的约束不冲突(它是展示状态指示,不是设置项)。

#### B9 · `+N` 徽标可交互 `P0`

**现状**:紧凑条放不下的渠道收进 `+N`,是纯文字,**点了没反应**。
**问题**:信息可见但不可达;用户知道有渠道被藏了却看不到。
**方案**:点击 `+N` → 展开为面板并滚动到第一个被隐藏的渠道、高亮 1 秒;悬停显示被隐藏渠道的列表 tooltip。
**收益**:截断不再是信息黑洞。

#### B10 · 阈值告警的行为出口 `P1`

**现状**:`notify` 开关未接线;行内只有颜色变化。
**问题**:用户不盯着窗口就会错过;而"盯着窗口"正是这个产品要避免的。
**方案**:①**托盘图标角标**显示最紧张渠道的百分比;②阈值跌破时状态点做一次 1.2s 呼吸动画(只播一次,不循环);③通知只在"跌破瞬间"发一次,持续低位期间不重复(避免骚扰)。
**收益**:告警闭环,且不打扰。

#### B11 · 行展开多开的取舍 `P2`

**现状**:单开(点开一行自动收起其它)。
**问题**:560px 高度下单开是合理默认,但"对比两个渠道的消耗"是真实场景。
**方案**:保持单开为默认,加「展开全部 / 收起全部」小按钮,或 `Alt+点击` 允许多开。
**收益**:覆盖对比场景,默认行为不变。

#### B12 · 原生对话框替换为自绘 `P0`

**现状**:删除密钥用 `confirm()`,失败用 `alert()`。
**问题**:WebView 原生对话框与界面风格完全脱节,`alert` 还会**阻塞**;在无边框透明置顶窗口上尤其突兀。
**方案**:删除改为行内确认(该行展开为「确认删除?[删除][取消]」);错误改为行内红色 `hint`,3 秒后淡出。
**收益**:交互一致;不再打断沉浸感。

#### B13 · 窗口位置持久化 + 多显示器自适应 `P0`

**现状**:形态持久化,但**窗口位置不持久化**,重启回到系统默认位置;显示器配置变化后窗口可能跑到屏外。
**问题**:常驻挂件的位置记忆是基本预期;每次开机都要重新摆一次位置会让人放弃使用。
**方案**:持久化 `last_position`(物理坐标 + 所属显示器标识);启动时校验是否仍在可见区域,否则居中到主显示器;监听显示器变化事件重新夹取位置。
**收益**:这是常驻挂件从"能用"到"愿意一直开着"的关键一步。

#### B14 · 管理页与展示页的切换过渡 `P2`

**现状**:`display:none / flex` 硬切。
**方案**:加 120ms 淡入 + 8px 位移过渡。
**收益**:空间关系更清楚。

#### B15 · 密钥保存的成功反馈 `P1`

**现状**:保存成功后清空输入框、重渲染;只有失败才提示。
**问题**:用户无法确认"确实存上了"以及"存的是哪把 key"。
**方案**:保存成功后该行短暂高亮(1.2s 绿色左边框)+ 显示尾号 —— 后端 `key_hint` 命令**已实现未接线**,只需前端调用。
**收益**:低成本、直接消除一类排查成本。

#### B16 · 键盘与全局快捷键 `P2`

**现状**:只有 `Esc`。
**方案**:形态切换 `1/2/3`、刷新 `R`、设置 `S`、展开当前行 `Space`;全局快捷键 `Alt+T` 唤起/隐藏(Tauri global-shortcut 插件)。
**收益**:重度用户效率提升;`Alt+T` 是原型稿交互说明里已有但未实现的承诺。

#### B17 · 托盘角标与菜单补全 `P1`

**现状**:托盘右键 = 显示面板 / 立即刷新 / 退出;左键切换显隐;**角标未实现**。
**方案**:菜单补「切换形态」子菜单与当前状态摘要(如「5 正常 · 1 预警」);角标显示最紧张渠道百分比。
**收益**:托盘成为完整的控制面,与 B10 配套。

### 10.3 优先级汇总

| 级别 | 条目 | 特征 |
|---|---|---|
| **P0**(10 条) | A3 A4 A7 A11 · B3 B6 B7 B8 B9 B12 B13 | 低成本高收益,或影响信息正确性/可发现性 |
| **P1**(12 条) | A1 A2 A5 A6 A8 A9 · B1 B2 B4 B5 B10 B15 B17 | 显著提升质感与效率,需要一定改动量 |
| **P2**(5 条) | A10 A12 · B11 B14 B16 | 打磨与合规 |

### 10.4 建议的落地顺序

1. **第一轮(纯打磨,不改结构)**:A4 对比度 → A7 滚动条 → B6 刷新反馈 → B9 `+N` 可交互 → B12 换掉 `confirm/alert` → A11 焦点态。全部是局部改动,风险极低,用户立刻能感知。
2. **第二轮(信息价值)**:B7 汇总环比 → B8 排序前置 → A3 状态点分层 → B13 位置持久化。
3. **第三轮(结构升级)**:A1 设计令牌 → A5 字号收敛 → A9 动效规范 → A6 卡片外阴影(需同步改验证脚本期望值)。
4. **第四轮(新能力)**:B5 点击穿透 → B2 吸附预览与分级展开 → B1 吸附预览线 → B4 轮播指示点 → A2 浅色主题 → B10/B17 告警闭环。

> **交叉提醒**:A6 会改变固定尺寸的标定值,`build/verify-form.js` 里的 `EXPECT` 与 `win-style.ps1` 的断言必须同步更新;A2 浅色主题落地前,先完成 A4 的对比度修正,否则浅色下的层级关系需要重新推导一遍。

---

## 11. 待定夺

| 项 | 选项 | 影响 |
|---|---|---|
| 单 exe 交付 | A. exe + `WebView2Loader.dll` 双文件(推荐) / B. 换 MSVC 工具链(本机无管理员权限,装不了) / C. 符号垫片强行静态链接(风险中高) | 影响交付形态与打包方式;详见 `PROGRESS.md` |
| 通知的实现方式 | Tauri notification 插件(系统级) / 托盘气泡 / 仅托盘角标 | 影响 B10 的实现路径与权限需求 |
| 是否引入构建工具链 | 当前前端是纯静态无打包器;引入 Vite 才能用真正的 token 文件与组件化 | A1 的"单一真源"目标在不引入构建工具的前提下只能用 CSS 变量近似 |
| 浅色主题的范围 | 仅展示面 / 展示面 + 管理页 / 全量(含托盘图标) | 影响 A2 的改动面 |

---

## 附录:文档维护约定

- 本文档记录**已实现的设计**,不记录设想。新功能落地后再补进来。
- 第 8 节「已开发功能清单」与第 9 节「差异台账」是**必更项** —— 每次功能合并都要同步。
- 第 10 节的优化建议落地后,从本节移除并把结论并入对应规范章节(而不是留在建议里)。
- 实现细节与踩坑记录写在 `PROGRESS.md`,本文档不重复。
